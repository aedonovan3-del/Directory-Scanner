from setuptools import setup, find_packages

setup(
    name="directory_scanner",
    version="0.1",
    packages=find_packages(),
    description="Minimal directory scanner that ranks files for automation using a perceptron-style score.",
)
